options(scipen=999)


if (!suppressWarnings(suppressMessages(require(pacman)))) {
        utils::install.packages("pacman", repos = "https://cran.ma.imperial.ac.uk")
}

pacman::p_load(shiny,
               rgdal,
               maptools,
               leaflet,
               dplyr,
               rgeos,
               sp,
	       raster,
	       spatialEco,
	       RColorBrewer,
               aws.s3)

print("Opening Visual Analytics Project!!!!!!!!!!!!!!!!!!!")